# ② テスト仕様 — このプロジェクト固有の指示

<!--
このファイルは `/legacy-2-testspec` が**毎回読む**プロジェクト個別の上乗せ指示です。
**人が著者**（AI は読むだけ・書き換えない）。

■ ここに書くと効くもの
   ケース観点の重点、期待値の決め方、命名、粒度、手本にする既存成果物、
   「毎回同じ指摘をしている」ことの予防

■ ここに書いても効かないもの（固定契約。矛盾する指示には従いません）
   情報遮断（②は legacy/ を読まない）／トレーサビリティマトリクス／
   期待値の根拠の区分（仕様書🟢 / 人間確認済み / ⚠未確定）／spec-hash ／
   人の承認ゲート／機械レビューの検査項目
   → 変えたいときは skill リポジトリの改版が必要です（開発者に相談）

■ 項目立て（節構成）は `docs/templates/test-spec.md`、
   丸め・許容誤差・モック方針などの規約は `docs/conventions.md` が正です。
   ここは「どう書くか」だけを書きます。

■ 空のまま（この案内だけ）なら「個別指示なし」として扱われます。
-->

## 重点（必ず入れてほしいケース観点）

<!-- 例: 0近傍と負値は全関数で必ず1ケース作る
     例: 配列は「長さ0・長さ1・上限」を機械的に入れる -->

## 期待値の決め方の約束

<!-- 例: 手計算した期待値は計算過程を「期待値の根拠」欄に残す
     例: 浮動小数の比較は conventions.md の許容誤差に必ず言及する -->

## ケース名・ID の付け方

<!-- 例: 正常系は -N01、異常系は -E01、境界は -B01 の連番にする -->

## 繰り返さないでほしい指摘

<!-- レビューで2回以上指摘したことをここに書く。
     例: グローバル状態の事後確認を書き忘れる -->

## 手本にするテスト仕様書

<!-- 例: docs/test-specs/F-0123.md -->
