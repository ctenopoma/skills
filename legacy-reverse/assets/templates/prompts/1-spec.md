# ① 仕様書 — このプロジェクト固有の指示

<!--
このファイルは `/legacy-1-spec` が**毎回読む**プロジェクト個別の上乗せ指示です。
**人が著者**（AI は読むだけ・書き換えない）。skill の共通手順に対して、
このプロジェクトでの重点・粒度・書き方の癖・繰り返したくない指摘を足す場所です。

■ ここに書くと効くもの
   書く順序・粒度・重点、用語と表記の統一、調査のコツ、手本にする既存成果物、
   「毎回同じ指摘をしている」ことの予防

■ ここに書いても効かないもの（固定契約。skill が優先し、矛盾する指示には従いません）
   情報遮断（読んでよい入力の範囲）／必須節／Confidence と根拠 file:lines の必須／
   ハッシュ連鎖／人の承認ゲート／機械レビューの検査項目／禁止事項
   → これらを変えたいときは skill リポジトリの改版が必要です（開発者に相談）

■ 項目立て（節構成）はここではなく `docs/templates/spec.md` が正です
   節を足す・減らす・改名するのはそちら。ここは「どう書くか」だけを書きます。

■ 空のまま（この案内だけ）なら「個別指示なし」として扱われます。
   節ごと削除してもかまいません。
-->

## 重点（このプロジェクトで特に丁寧に書いてほしいこと）

<!-- 例: 単位系の変換は必ず式で書く／配列添字の基準（0始まり・1始まり）を毎回明記する -->

## 用語・表記の約束

<!-- 例: 「税率区分」は "rate_code" と書く（"区分コード" は使わない）
     例: 数式は LaTeX でなくプレーンな式で書く -->

## 繰り返さないでほしい指摘

<!-- レビューで2回以上指摘したことをここに書く。次回から予防される。
     例: 処理フロー図に SPEC-ID を添え忘れる
     例: 「〜を計算する」で終わらせず、丸めの方向まで書く -->

## 手本にする仕様書

<!-- 例: docs/specs/F-0123.md（粒度と根拠の付け方はこれに揃える） -->
