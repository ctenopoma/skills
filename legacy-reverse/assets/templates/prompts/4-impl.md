# ④ 実装 — このプロジェクト固有の指示

<!--
このファイルは `/legacy-4-impl` が**毎回読む**プロジェクト個別の上乗せ指示です。
**人が著者**（AI は読むだけ・書き換えない）。

■ ここに書くと効くもの
   実装の構造・分割の方針、使ってよい/避けるライブラリと API、性能上の注意、
   手本にする既存モジュール

■ ここに書いても効かないもの（固定契約。矛盾する指示には従いません）
   情報遮断（④は legacy/・②・tests/ を読まない）／①の signature に従うこと／
   スタブ・仮実装の禁止（詰まったら spec-gap ISSUE で停止）
   → 変えたいときは skill リポジトリの改版が必要です（開発者に相談）

■ 型対応表・docstring 規約・命名・禁止事項は `docs/conventions.md` が正です。
   ここは conventions.md に書ききれない「実装の癖」を足す場所です。

■ 空のまま（この案内だけ）なら「個別指示なし」として扱われます。
-->

## 実装の方針（構造・分割）

<!-- 例: 1関数=1モジュールを維持し、共通処理は src/<pkg>/_util.py へ切り出す
     例: レガシーの GOTO ループは while ではなく for + break で書き直す -->

## 使ってよい / 避けるライブラリ・API

<!-- 例: 数値配列は NumPy を使ってよい（要素ごとの Python ループは避ける）
     例: Decimal を float に変換しない（丸め規則が変わるため） -->

## 性能・メモリの注意

<!-- 例: 入力配列は最大 10^7 要素。全要素コピーを伴う実装は避ける -->

## 繰り返さないでほしい指摘

<!-- 例: docstring の Spec: 行に func-id を書き忘れる -->

## 手本にするモジュール

<!-- 例: src/newpkg/tax.py -->
