# ③ テストコード — このプロジェクト固有の指示

<!--
このファイルは `/legacy-3-testcode` が**毎回読む**プロジェクト個別の上乗せ指示です。
**人が著者**（AI は読むだけ・書き換えない）。

■ ここに書くと効くもの
   テストコードの書き方（fixture・パラメータ化・命名・ファイル分割）、
   使ってよい/避けるライブラリ、手本にする既存テスト

■ ここに書いても効かないもの（固定契約。矛盾する指示には従いません）
   情報遮断（③は legacy/・①仕様書・src/ を読まない）／1ケース=1テスト関数と
   `@pytest.mark.tc("<ケースID>")` の付与／②に無いケースを足さないこと／
   freeze 後のテスト改変禁止
   → 変えたいときは skill リポジトリの改版が必要です（開発者に相談）

■ 型対応・モックの基本方針・テストIDの対応付けは `docs/conventions.md` が正です。
   ここは conventions.md に書ききれない「書き方の癖」を足す場所です。

■ 空のまま（この案内だけ）なら「個別指示なし」として扱われます。
-->

## テストコードの書き方

<!-- 例: 同値分割のケースは pytest.mark.parametrize でまとめてよい（ケースIDは個別に付ける）
     例: fixture は tests/conftest.py に集約し、テストファイル内に定義しない -->

## 使ってよい / 避けるライブラリ

<!-- 例: 数値比較は pytest.approx を使う（自前の abs 差分比較は書かない）
     例: hypothesis は使わない（レビューコストのため） -->

## ファイル分割・命名

<!-- 例: 1関数=1ファイル tests/test_<module>.py -->

## 繰り返さないでほしい指摘

<!-- 例: 事後のグローバル状態を assert し忘れる -->

## 手本にするテストファイル

<!-- 例: tests/test_tax.py -->
