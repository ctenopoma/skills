---
title: "テスト仕様書: {{new_name}}"
func-id: "{{func_id}}"
spec-ref: "specs/{{func_id}}.md"
spec-hash: "{{spec_hash}}"   # 生成時点の①のハッシュ。①が改訂されたら不一致→本書は「要再生成」
status: generated             # generated(LLM生成済) → approved(人が承認。③はapprovedのみ着手可)
approved-by: null
approved-date: null
---

<!-- LR:TEMPLATE-NOTE
このテンプレはプロジェクトの所有物（docs/templates/test-spec.md。人が編集する）。
項目立てと書き方ガイド（コメント）は自由に変更してよい。②はこの形式に従って書き、
review_checks が「# 見出し」を必須節として検証する。
固定契約（削除・改名できないアンカー）: # トレーサビリティマトリクス、
ケースID <func_num>-TC-NNN、「対応仕様」「期待値の根拠」の行、期待値の根拠の語彙
（仕様書🟢 / 人間確認済み / レガシー実測 / ⚠未確定）。
-->

# テスト方針

<!--
- 同値分割・境界値の導出方針
- モック方針（グローバル状態・外部ファイル・サブルーチンをどう差し替えるか）
- 期待値の確定方法。レガシー実行環境なしが基本前提のため、期待値は 仕様🟢 と 人間確認 で確定する。
  環境がある場合のみゴールデンマスター（実測値）を第一ソースにできる
-->

# テストケース

<!--
ケースIDは <func_num>-TC-<3桁連番>。
「期待値の根拠」は必ず次のいずれか:
  仕様書🟢 / 人間確認済み / ⚠未確定 （レガシー実行環境がある場合のみ「レガシー実測」も可）
⚠未確定のケースには必ず「質問」欄を付け、LLMの仮説＋Yes/Noで答えられる問いを書く（人の負荷を下げる）。
-->

## {{func_num}}-TC-001: {{目的を一行で}} {#tc-001}

| 項目 | 内容 |
|------|------|
| 分類 | 正常系 / 境界値 / 異常系 / 外部ファイルI/O / グローバル状態 / 副作用 |
| 対応仕様 | [SPEC-{{func_num}}-01](../specs/{{func_id}}.md#spec-{{func_num_lower}}-01) (🟢) |
| 事前条件 | |
| 入力 | |
| 期待結果 | <!-- 戻り値＋事後グローバル状態＋ファイル書き込み＋副作用まで --> |
| 期待値の根拠 | |
| サブルーチン | MOCK: — ／ REAL: — |

# トレーサビリティマトリクス

<!-- 完了条件（機械チェック）: 全🟢仕様項目にケース1件以上 ＋ ⚠未確定ゼロ ＋ status: approved -->

| 仕様項目 | Confidence | ケース |
|---------|:---:|------|
| SPEC-{{func_num}}-01 | 🟢 | TC-001 |

# 未確定事項

| ISSUE | 対象ケース | 質問（LLMの仮説＋Yes/No） | 状態 |
|-------|-----------|--------------------------|------|
| | | | |
