---
title: "テスト結果: {{new_name}}"
func-id: "{{func_id}}"
test-spec-ref: "test-specs/{{func_id}}.md"
test-code-hash: "{{test_code_hash}}"  # ③承認時のハッシュと照合。不一致＝テストコード改変→実行前に停止
run:
  date: "{{run_date}}"
  commit: "{{commit}}"
  env: "{{env}}"
result: fail            # pass / fail。判定は「実装率100% かつ 失敗0」
attempt: 1              # ④⑤ループの試行回数。上限(既定:3)到達で自動ISSUE起票(kind:triage)→人にエスカレーション
                        # 人の裁定(ISSUEがapplied)後に人が⑤を再トリガした時点で 1 にリセット（前提が変わったため）
blocked-by: null        # 上限到達時に起票した ISSUE-ID。null 以外の間、④⑤の再実行は skill が拒否する
tc-coverage:            # 「coverage」はQuartoの予約キーと衝突するため tc- 接頭辞
  spec-cases: 0         # ②に定義されたケース数
  implemented: 0        # ③に実装が存在するケース数（マーカー @pytest.mark.tc で対応付け）
  rate: "0%"            # implemented / spec-cases
---

<!--
ファイル名: test-results/{{func_id}}_{{YYYYMMDD-HHMMSS}}.md
実行のたびに新規ファイルを1から自動生成する（上書き・手編集禁止）。
本書はテストランナーのJSON出力＋②のケース一覧の突合で自動生成する。
WBS・⑥は同一 func-id の最新ファイルを参照する。
「試行履歴」は同一 func-id の過去ファイルを走査して自動生成する。
-->

# サマリ

| 仕様ケース数 | 実装済み | **実装率** | 成功 | 失敗 | スキップ | 実行時間 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| | | | | | | |

<!-- 実装率 = ②のケースIDのうち、③のテストコードにマーカーが存在するものの割合。
     100%未満のうちは失敗0でも result: fail（未実装ケースの一覧を下表に出す） -->

# ケース別結果

<!-- ②の全ケースを必ず行に出す。③に実装が無いケースは 結果=－(未実装) と明示する -->

| ケースID | 実装 | 結果 | 時間 | 詳細 |
|---------|:---:|:---:|-----:|------|
| {{func_num}}-TC-001 | ✅ | ✅ | 0.01s | |
| {{func_num}}-TC-002 | － | 未実装 | — | |

# 失敗詳細

<!-- 失敗ケースごとに。トリアージ分類は ⑤の裁定ルールに従う:
  (a) 実装バグ → ④で修正（承認不要・デフォルト）
  (b) テストコードバグ → ISSUE起票→人の承認後③修正
  (c) 仕様誤り → ISSUE起票→人の承認後②(①)修正→ハッシュ連鎖で③以降を無効化
-->

## {{func_num}}-TC-003 {#fail-tc-003}

- expected:
- actual:
- ログ抜粋:
- トリアージ分類（暫定）: (a) / (b) / (c) — 根拠:

# 試行履歴

| 試行 | 日時 | 実装率 | 結果 | 修正内容の要約 |
|:---:|------|:---:|:---:|----------------|
| 1 | | | | |
