---
title: "{{要約を一行で}}"
issue-id: "ISSUE-{{3桁連番}}"
status: open            # open → answered(人が回答) → applied(成果物へ反映済) / rejected
kind: spec-gap          # spec-gap(仕様の穴) / domain(ドメイン知識) / legacy-bug(レガシーのバグ疑い) / triage(⑤の裁定待ち) / other
func-id: "{{func_id}}"  # 関連関数。複数可
raised-in: "phase-2"    # 発見フェーズ: phase-0 〜 phase-6
raised-date: "{{date}}"
---

# 事象

<!-- 何を発見したか。根拠（file:lines、実測値）付きで -->

# 質問（人への問い）

<!-- LLMの仮説を提示し、Yes/No/修正で答えられる形にする。白紙質問は禁止 -->

**仮説**:

**問い**: この理解で正しいですか？

# 回答（人が記入）

- 回答:
- 回答者 / 日付:

# 反映

<!-- answered→applied にするとき記入。どの成果物をどう直したか。domain-knowledge.md への転記要否 -->

| 反映先 | 内容 |
|--------|------|
| | |
